# De ejemplo para configurar las variables de entorno necesarias para la aplicación.
# Solo quita el .example y completa los valores para usarlo (caso se lleguen a usar).

DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=