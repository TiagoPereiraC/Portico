services:
  php:
    build: .
    container_name: portico-php
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file:
      - .env
    volumes:
      - ./api:/var/www/html/api
      - ./web-ui:/var/www/html/web-ui
